namespace TallerEstructurado;

public class Ejercicio5
{
    /*static void Main(string[] args)
    {
        // 5.	Lea la distancia (en kilómetros) recorrida por un auto, el tiempo (en horas) en que la recorrió y calcule la velocidad a la cual se desplazaba el auto (V=D/T).
        Console.WriteLine("Digite la distancia recorrida en kilometros");
        double distancia = double.Parse(Console.ReadLine());
        Console.WriteLine("Digite el tiempo en horas");
        double tiempo = double.Parse(Console.ReadLine());
        double velocidad = distancia / tiempo;
        Console.WriteLine($"La velocidad a la que se desplazaba el auto es: {velocidad} km/h");
    }
    */
}