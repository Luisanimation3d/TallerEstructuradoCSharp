namespace TallerEstructurado;

public class Ejercicio14
{
    /*static void Main(string[] args)
    {
        // 14.	Se trata de escribir el algoritmo que permita emitir la factura correspondiente a una compra de varios artículos (4) determinados, del que se adquieren una o varias unidades. El IVA es del 19%.
        double valorTotal = 0;
        for (int i = 0; i < 4; i++)
        {
            Console.WriteLine("Digite el valor del artículo");
            double valorArticulo = double.Parse(Console.ReadLine());
            Console.WriteLine("Digite la cantidad del artículo");
            int cantidadArticulo = int.Parse(Console.ReadLine());
            valorTotal += valorArticulo * cantidadArticulo;
        }
        double iva = valorTotal * 0.19;
        double valorFinal = valorTotal + iva;
        Console.WriteLine($"El valor total es: {valorTotal}");
        Console.WriteLine($"El valor del IVA es: {iva}");
        Console.WriteLine($"El valor final es: {valorFinal}");
    }*/
}