namespace TallerEstructurado;

public class Ejercicio6
{
    /*
    static void Main(string[] args)
    {
        // 6.	Lea la cantidad de dinero correspondiente a una compra y calcule el valor del IVA (19%), y el valor total de la factura, si al valor de la compra se le autoriza un descuento del 10% (antes de aplicarle el IVA).
        Console.WriteLine("Digite el valor de la compra");
        double valorCompra = double.Parse(Console.ReadLine());
        Console.WriteLine("Se le autoriza descuento del 10%? (S/N)");
        string respuestaDescuento = Console.ReadLine();
        if (respuestaDescuento == "S")
        {
            double descuento = valorCompra * 0.1;
            double valorCompraConDescuento = valorCompra - descuento;
            double iva = valorCompraConDescuento * 0.19;
            double valorTotal = valorCompraConDescuento + iva;
            Console.WriteLine($"El valor del descuento es: {descuento}");
            Console.WriteLine($"El valor del iva es: {iva}");
            Console.WriteLine($"El valor total de la factura es: {valorTotal}");
        }
        else
        {
            double iva = valorCompra * 0.19;
            double valorTotal = valorCompra + iva;
            Console.WriteLine($"El valor del iva es: {iva}");
            Console.WriteLine($"El valor total de la factura es: {valorTotal}");
        }
    }
    */
}